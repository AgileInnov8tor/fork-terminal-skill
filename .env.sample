# Fork Terminal Skill - Environment Variables Template
# Copy this file to .env and fill in your API keys

# Anthropic API Key (for Claude Code CLI)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Google API Key (NOT needed for Gemini CLI)
# Gemini CLI uses Google account subscription authentication
# Run `gemini` and it will prompt for Google account login
# GOOGLE_API_KEY is only needed for Google AI API (not CLI)
# GOOGLE_API_KEY=your-google-api-key-here

# Codex API Key (if using Codex CLI)
# Get your API key from your Codex CLI provider
CODEX_API_KEY=your-codex-api-key-here

# Note: Never commit your actual .env file to version control
# The .gitignore file is configured to exclude .env files
